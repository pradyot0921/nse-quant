# Data Directory

Raw files under `data/raw/` are immutable source files.

Processed, normalised, and adjusted datasets belong under `data/processed/`.

Do not commit private broker records, credentials, or sensitive account data.
